<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false">

	<!--使用springProfile标签设置不同环境的日志 使用logback-spring.xml命名日志文件支持使用该标签-->
	<springProfile name="development">
		<property name="logDir" value="./logs" />
	</springProfile>

	<springProfile name="integration">
		<property name="logDir" value="./logs" />
	</springProfile>
      
    <springProfile name="production">
        <property name="logDir" value="/home/log/boot" />
    </springProfile>

	<!--控制台日志-->
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>
				%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
			</pattern>
		</encoder>
	</appender>

	<appender name="INFO" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<encoder>
			<pattern>
				%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
			</pattern>
		</encoder>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>
				${logDir}/infoLog.%d{yyyy-MM-dd}.log
			</fileNamePattern>
			<maxHistory>30</maxHistory>
		</rollingPolicy>
		<filter class="ch.qos.logback.classic.filter.LevelFilter">
			<level>INFO</level>
			<onMatch>ACCEPT</onMatch>
			<onMismatch>NEUTRAL</onMismatch>
		</filter>
	</appender>

	<appender name="ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<encoder>
			<pattern>
				%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
			</pattern>
		</encoder>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>
				${logDir}/errLog.%d{yyyy-MM-dd}.log
			</fileNamePattern>
			<maxHistory>30</maxHistory>
		</rollingPolicy>
		<filter class="ch.qos.logback.classic.filter.LevelFilter">
			<level>ERROR</level>
			<onMatch>ACCEPT</onMatch>
			<onMismatch>DENY</onMismatch>
		</filter>
	</appender>

	<root level="INFO">
		<springProfile name="!production">
			<appender-ref ref="STDOUT" />
		</springProfile>
		<appender-ref ref="INFO" />
		<appender-ref ref="ERROR" />
	</root>
	<springProfile name="!production">
		<logger name="mybatis" level="DEBUG"/>
	</springProfile>
</configuration>  